<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/rl_chain"
    android:layout_margin="@dimen/google_1x">

    <EditText
        android:id="@+id/et_contacts_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toStartOf="@id/img_contacts_search"
        android:layout_margin="@dimen/google_1x"
        android:hint="@string/search_hint"
        />

    <ImageView
        android:id="@+id/img_contacts_search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/search"
        android:layout_alignParentEnd="true"
        android:layout_margin="@dimen/google_1x"
        />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_for_contacts"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/et_contacts_search"
        android:layout_margin="@dimen/google_1x"
        >

    </android.support.v7.widget.RecyclerView>

</RelativeLayout>